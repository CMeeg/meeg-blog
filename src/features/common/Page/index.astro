---
import type { PageStory } from '@features/common/api'
import Bloks from '@features/common/Bloks/index.astro'

export interface Props {
  story: PageStory
}

const { story } = Astro.props as Props
---

<Bloks bloks={story.content.body} />
