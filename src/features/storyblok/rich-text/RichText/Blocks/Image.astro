---
import type { AssetStoryblok } from '../../../types/components'
import { getImageAssetUrl } from '../../../url'

export interface Props {
  asset: AssetStoryblok
}

const { asset } = Astro.props as Props

const src = getImageAssetUrl(asset)

const { alt, title } = asset
---

<!-- TODO: Use Astro Image component? -->
<img src={src} alt={alt} title={title} />
