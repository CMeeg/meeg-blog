---
import type { MultilinkStoryblok } from '../../../types/components'
import { getLinkUrl } from '../../../url'

export interface Props {
  link: MultilinkStoryblok
}

const { link } = Astro.props as Props
---

<a href={getLinkUrl(link)}><slot /></a>
