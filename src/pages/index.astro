---
import { getStory } from '@features/storyblok/api'
import type { PageStory } from '@features/storyblok/types/content-types'
import HomePage from '@features/home/HomePage/index.astro'

const story = await getStory<PageStory>('home')

if (!story) {
  // TODO: Deal with this - 404
  throw Error(`'home' story not found.`)
}
---

<HomePage story={story} />
